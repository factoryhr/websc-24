# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - Db:
              dsn: 'mysql:host=mysql:3306;dbname=project'
              user: 'wsc'
              password: 'WSC2024.!'
        - Symfony:
              app_path: 'src'
              #            environment: 'test'
              kernel_class: 'App\Kernel'

step_decorators: ~
